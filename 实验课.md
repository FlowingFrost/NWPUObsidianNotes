# 实验1
...继续
### 表格3
题目：创建一个五行三列的复杂表格，其中包括合并行与列的操作，并且要求不同的文本对齐方式。表格应如下所示：
*注：markdown本身不支持合并单元格，我通过将不同单元格填入相同内容来表示它需要合并*

| Header 1        | Header 2             | Header 2             |
| --------------- | -------------------- | -------------------- |
| Row 1           | Merge Across Columns | Merge Across Columns |
| Row 2           | Data A               | Data B               |
| Merge Down Rows | Data C               | Data D               |
| Merge Down Rows | Data E               | Data F               |

表3：复杂表格与单元格合并
·第一行：Header 2合并了第二和第三列。
·第二行：Merge Across Columns 合并了第二和第三列。
●第四行：Merge Down Rows 合并了两行的第一列。提示：
1.使用（multicolumn{}合并列。
2.使用（multirow）合并行。
3.使用适当的对齐符号（1，c，r）调整文本对齐。
### 实验题：表格4（选做）
题目：
创建一个7行4列的复杂表格，要求：
1．第一行的标题行跨四列，且文字居中。
2.第二行的第一列内容合并两行，且文字居左。
3.第三行的第二列与第三列合并。
4.在第五行的第四列嵌套一个小表格，显示2x2的数据，并确保表格宽度适中。
5.所有列的宽度需要调整，第二列和第三列占总宽度的50%。
提示：
1.使用\multicolumn 合并标题行和列。
2.使用\multirow合并单元格行。
3.使用p{width} 控制列宽，确保表格美观。
4.嵌套表格要适当调整大小，并控制布局。

| Complex Table Title | Complex Table Title    | Complex Table Title    | Complex Table Title                |
| ------------------- | ---------------------- | ---------------------- | ---------------------------------- |
| Row 1               | Cell 1                 | Cell 2                 | Cell 3                             |
| Row 1               | Merge Across 2 Columns | Merge Across 2 Columns | Cell 3*这里是例外，第二个Cell3，不清除是不是作者写错了* |
| Row 2               | Cell 4                 | Cell 5                 | Cell 6                             |
| Row 3               | Cell 7                 | Cell 8                 | 4格，N1 N2 N3 N4                     |

## 图片1
### 题目：
插入一张图片，并为其添加标题。使用占位符图片example-image-a来代替真实图片。
提示：
使用graphicx宏包来插入图片。
使用\includegraphics来指定图片路径。图片标题使用\caption{}。
代码框架：
latex
复制代码
\documentclass{article]\usepackage{graphicx)
\begin{document}
\begin{figure}[h!]\centering%插入图片代码\caption{示例图片}
\end{figure}
\end{document}
### 2.中级题目：调整图片大小与位置题目：
插入一张图片，并将其大小调整为文档宽度的60%，同时让图片居右显示。
提示：
·使用width参数调整图片宽度。·使用\raggedleft使图片居右对齐。
代码框架：
latex
复制代码
\documentclass{article}\usepackage{graphicx}
\begin{document}
\begin{figure}[h!]\raggedleft%插入图片代码
\caption{调整大小与位置的图片}\end{figure}
\end{document}
### 题目：
插入两张图片，并将它们并排显示，且每张图片占据50%的文档宽度。每张图片需有独立的标题。
提示：
使用minipage环境将图片并排显示。分别设置每张图片的宽度为50%。
代码框架：latex
复制代码
\documentclass[article]\usepackage(graphicx)
\begin{document}
(begin{figure}[h!]\centering
\begin{minipage}[b]{o.5\textwidth}%插入第一张图片代码
\caption{左侧图片}\end{minipage)
\begin{minipage}[b]{o.5\textwidth}%插入第二张图片代码
\caption{右侧图片}\end{minipage)
\end{figure}
\end{document}
### 题目：
在文档中插入一张图片，图片占据文档右侧的40%，左侧的60%放置一段文字描述。要求图片与文字并排显示。
提示：
使用minipage环境创建左右并排的区域。
通过设置每个minipage的宽度来控制图片与文字的比例。
代码框架：
latex
(documentclass{article)(usepackage[graphicx}
(begin{document}
(begin(figure}[h!]\centering
(begin{minipage}[b](0.6\textwidth]x在这里输入文字描述
\end{minipage}
(begin{minipage}[b](0.4\textwidth]x插入图片代码
\caption[右侧图片}\end{minipage}
\end{figure}
\end{document}

# 实验2
## 实验题：列表与项目符号的使用
### 题目：列表与项目符号的综合使用
要求：
1．创建一个无序列表，列出三个日常活动。
2．创建一个嵌套的有序列表，外层使用数字编号，内层使用字母编号，列出三个任务及其详细步骤。
3．自定义列表符号，创建一个自定义的嵌套列表，使用”星号"作为符号，并自定义内层符号为"加号"
提示：
1.初级部分：使用itemize 环境创建无序列表。
2.中级部分：使用enumerate 环境创建有序列表，并嵌套enumerate 实现字母编号。
3.高级部分：使用enumitem宏包来自定义列表符号，并调整缩进和符号样式。
代码框架：
latex
\documentclass{article}\usepackage{enumitem}
\begin{document}
%初级部分：无序列表\section{无序列表}\begin{itemize}
%在这里补全无序列表lend{itemize}
%中级部分：嵌套有序列表\section{嵌套有序列表}\begin{enumerate}
%在这里补全外层有序列表\begin{enumerate}
%在这里补全内层有序列表lend{enumerate}
lend{enumerate}
%高级部分：自定义符号列表\section{自定义符号列表}
\begin{itemize}[label=$\ast$]\item自定义外层符号\begin{itemize}[label=$+$]
%在这里补全内层自定义符号\end{itemize}
lend{itemize}
\end{document}
## 代码排版与算法描述
### 1.初级题目：插入Python代码题目：
使用 1istings 宏包插入一段简单的Python 代码，内容为一个函数 add（a，b），返回两个数的和。
提示：
使用listings宏包格式化代码。
def add(a, b):
使用\begin{lstlisting}环境插入代码块。
return a + b
代码框架：latex
复制代码
\documentclass{article}\usepackage{listings}
\begin{document}
\section{插入Python代码}
%在这里插入Python代码
\end{document}

### 2.中级题目：排序算法的描述题目：
使用algorithm宏包描述一个简单的冒泡排序算法，并为每一步加上说明。算法应包括输入、输出、步骤和算法结束条件。
提示：
·使用algorithm 和algorithmic 宏包来格式化算法。·使用\STATE 来表示算法中的每个步骤。
代码框架：latex
复制代码
\documentclass{article}\usepackage{algorithm}\usepackage{algorithmic}
\begin{document}
\section{冒泡排序算法}
\begin{algorithm}
\caption{冒泡排序算法}\begin{algorithmic}[1]
%在这里补全算法步骤\end{algorithmic}
lend{algorithm}
lend{document}
*注：右侧有配图，内容如下*
```配图表格
Algorithm1冒泡排序算法
---
1:输入：数组A，长度 n
2:输出：排序后的数组A
3:for i = 1 to n - 1 do
4:  for j = 1 to n - i do
5:    if A[j] > A[j+1] then
6:      交换 A[j] 和 A[j+1]
7:    end if
8:  end for
9:end for
10：返回排序后的数组A
```

### 3.高级题目：带注释的代码高亮与自动换行题目：
插入一个带有注释的Python代码片段，要求代码能够自动换行，且高亮显示关键字（如def、return）。注释应使用不同的颜色。
提示：
· 使用listings宏包的breaklines选项来实现代码自动换行。
· 使用keywordstyle和commentstyle来设置代码高亮和注释颜色。
代码框架：
\documentclass{article}
\usepackage{listings}
\usepackage{xcolor}
\begin{document}
\section{带注释的Python代码}
\begin{lstlisting}[language=Python, breaklines=true, keywordstyle=\color{blue}, commentstyle=\color{green}]
#在这里插入Python代码
\end{lstlisting}
\end{document}

*注：右侧有配图，内容如下*
```配图
<font color="#2DC26B">#这是一个简单的加法函数</font>
<font color="反正是蓝色">def</font> add(a, b):
<font color="#2DC26B">#返回a和b的和</font>
<font color="一样的蓝色">return</font> a + b
```
### 代码排版与算法描述（选做）
高阶题目：自定义代码样式与算法伪代码的结合题目：
在文档中插入两个部分：
1.定义一个自定义代码样式，用来高亮显示Python代码。该样式需要具备以下特性：·关键字（如 def，return）高亮为蓝色。
·字符串（如“Hello"，‘world'）高亮为红色。·注释高亮为绿色。
·自动换行。
2.使用algorithm宏包描述一个递归实现的二分查找算法。要求：·算法应使用伪代码描述，并标明每个步骤的注释。
·算法必须包含递归调用部分，以及返回条件。
提示：
·使用 listings 宏包的\lstdefinestyle 定义自定义代码样式。
·使用algorithm和algorithmic宏包描述二分查找算法，确保递归逻辑的正确性。
代码排版与算法描述（选做）*注：这里是对上面题目给出的示例*
5.4.1自定义代码样式
#这是一个递归计算阶乘的Python函数
def factorial(n):
if n == 0:
return
else:
return n * factorial(n-1)
5.4.2二分查找算法
Algorithm2递归二分查找算法1：输入：已排序数组A，查找元素x2:输出：元素x的位置（如果存在）
3:定义递归函数binarySearch(A,left,right,x):4: if left > right then
5:
return-1{查找失败，返回-1}6: end if
7:mid=left+right8: if A[mid] == x then
2
9:
returnmid{元素找到，返回位置}
10: else if A[mid] > x then
11:
return binarySearch（A,left，mid-1,x）{递归查找左半部分}
12: else
13:
return binarySearch（A，mid+1,right，x）{递归查找右半部分}
14: end if

## 颜色与背景1
### 1.初级题目：给文字添加颜色题目：
为一段文字加上红色的字体颜色。文字内容为“这是一个红色的段落”
提示：
使用xcolor宏包来设置文本颜色。
使用\textcolor{}命令来为特定文本添加颜色。
### 2.中级题目：表格背景色设置题目：
创建一个包含四行的表格，并为表格的偶数行（第二行和第四行）设置浅灰色背景。
提示：
·使用xcolor宏包的table选项来控制表格行的背景颜色。使用\rowcolors命令为偶数行设置背景色。
### 3.高级题目：文档背景颜色与水印题目：
为整个文档设置浅黄色的背景颜色，并在页面中央添加"机密“水印，水印使用灰色半透明效果。
提示：
·使用pagecolor命令设置页面背景颜色。
·使用background 宏包插入水印，并设置透明度。


# 实验3
## 绘图
### 1.绘制一个坐标系中的三角形
任务：使用Tikz绘制一个在二维坐标系中的三角形，并标注每个顶点的坐标。
提示：
·使用\draw 命令绘制三角形的边。·使用\node 命令标注顶点。

(1,3)
(3,1)
(0,0)
### 2.绘制带网格和标注的函数$y=x^2$
任务：绘制 $y = x^2$ 的函数图，并为坐标轴加上网格和标注。
提示：
·使用pgfplots 宏包，\begin{axis} 绘制函数图。·启用网格，并为坐标轴加上标签。

### 3.绘制流程图
任务：绘制一个简单的流程图，包括"开始”、“处理”、“结束"三个步骤，并使用箭头连接它们。
提示：
·使用\node 和\draw[->] 绘制流程图中的节点和箭头。
开始
处理
结束

### 4.绘制一棵二叉树
任务：使用Tikz绘制一棵简单的二叉树，树包含3层，根节点的值为1，每个节点的子节点分别为其值乘以2和值乘以2加1。
提示：
·使用\node 和\draw构建树的结构。





### 5.绘制正弦和余弦函数的叠加图
任务: 绘制正弦函数 $y = \sin(x)$ 和余弦函数 $y = \cos(x)$， 并将它们叠加显示在同一图表中。提示：
· 使用 pgfplots 同时绘制两条函数曲线，并为它们加上图例。

### 6.绘制三维曲面图
任务:使用 pgfplots 绘制三维曲面图，曲面为 $z=x^2 +y^2$。
提示：
·使用\begin{axis}[3d]环境绘制三维图，并设置网格样式。