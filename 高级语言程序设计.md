# 高级语言程序设计

## 课程信息
- **试卷构成**：Java 50% + UML 50%
- **成绩构成**：期末70% + 平时30%
- **常见设计模式**：工厂模式、单例模式、适配器模式

---

## 第1章 Java语言基础

### 1.1 Java vs C# 关键差异

| 特性 | Java | C# |
|------|------|-----|
| 预处理 | 无，用import | 无，用using |
| 结构体 | 无struct/union | 有struct |
| 指针 | 无 | 有unsafe模式 |
| goto | 保留字但禁用 | 可用 |
| 操作符重载 | 不支持 | 支持 |
| 全局变量 | 无 | 无 |
| 多重继承 | 不支持（用接口） | 不支持（用接口） |

### 1.2 JVM相关
- **字节码**：.class文件，跨平台
- **JIT**：即时编译，提高性能
- **混合模式**：解释执行+编译执行

### 1.3 数据类型差异

| 类型 | Java | C# |
|------|------|-----|
| 布尔 | boolean (1字节) | bool (1字节) |
| 字符 | char (2字节，Unicode) | char (2字节) |
| 长整型 | long (8字节) | long (8字节) |
| 字符串 | String（不可变引用类型） | string（不可变引用类型） |

**Java特有**：
- boolean只能true/false，不能当整数用
- 没有无符号类型（C#有byte, uint, ulong等）

### 1.4 运算符差异
- `>>>` 无符号右移（Java特有）
- `instanceof` 类型检查（C#是is）
- `==` 比较对象引用（C#比较值，需用Equals）
- `String`拼接是唯一"运算符重载"

### 1.5 数组
```java
int[] arr = new int[10];      // 声明+分配
int[] arr2 = {1, 2, 3};       // 初始化
arr.length                     // 属性，不是方法
```
C#用`arr.Length`，且有`Array.Resize`等方法。

### 1.6 包机制
```java
package cn.edu.nwpu;           // 类似C#的namespace
import java.util.*;            // 类似C#的using
import static java.lang.Math.*; // 静态导入（C#无此特性）
```

**静态导入**：可直接用`sqrt()`而不用`Math.sqrt()`

### 1.7 源文件规则
- 一个.java文件可有多个类，但**最多一个public类**
- public类名必须与文件名相同
- 编译后每个类生成一个.class文件

---

## 第2章 控制语句

### 2.1 与C#的差异

**基本相同**：if/else, switch, while, do-while, for, break, continue

**差异点**：
1. **变量作用域**：Java不允许嵌套块内同名变量
   ```java
   {
       int k = 5;
   }
   {
       int k = 6;  // C#允许，Java编译错误
   }
   ```

2. **switch**：Java支持String（JDK7+）

3. **foreach**：语法相同
   ```java
   for (int item : arr) { }
   ```

### 2.2 异常处理
与C#几乎相同，关键字差异：
- Java: `try-catch-finally`, `throws`, `throw`
- C#: `try-catch-finally`, `throws`, `throw`

**Java检查型异常**：编译器强制处理（C#无此概念）
- IOException, SQLException等必须try-catch或throws
- RuntimeException及其子类不强制处理

### 2.3 数组操作
```java
int[] arr = new int[5];
arr.length                    // 属性
Arrays.sort(arr);             // 排序
Arrays.toString(arr);         // 转字符串
System.arraycopy(src, 0, dst, 0, len); // 复制
```

C#用`Array.Sort()`, `Array.Copy()`等方法。

---

## 第3章 函数（方法）

### 3.1 静态方法 vs C#静态方法
- 语法相同：`public static void Method() { }`
- 调用：`ClassName.Method()` 或同类内直接`Method()`
- 静态方法只能访问静态成员（与C#相同）

### 3.2 参数传递
**Java全是值传递**：
- 基本类型：复制值
- 引用类型：复制引用（地址），但引用本身是值传递

```java
void modify(int x, int[] arr) {
    x = 100;        // 不影响原变量
    arr[0] = 100;   // 影响原数组
}
```

C#有`ref`/`out`关键字可传递引用，Java没有。

### 3.3 方法重载
- 与C#规则相同：方法名相同，参数列表不同
- **不包括返回值类型**（C#也一样）

### 3.4 可变参数
```java
void method(int... args) { }  // Java
void method(params int[] args) { }  // C#
```
语法不同，功能相同。

### 3.5 命令行参数
```java
public static void main(String[] args) {
    // args[0], args[1]...
}
```
C#是`static void Main(string[] args)`

### 3.6 Javadoc注释
```java
/**
 * 方法描述
 * @param name 参数描述
 * @return 返回值描述
 * @throws Exception 异常描述
 */
```
类似C#的XML文档注释`/// <summary>`

### 3.7 静态导入（C#无此特性）
```java
import static java.lang.Math.*;
// 可直接用 sqrt(), PI 而不用 Math. 前缀
```

---

## 第4章 对象和类

### 4.1 访问控制差异

| 修饰符 | Java | C# |
|--------|------|-----|
| public | 相同 | 相同 |
| private | 相同 | 相同 |
| protected | 同包+子类 | 仅子类 |
| 默认(包访问) | 同包可见 | 无此概念 |

**Java特有**：包访问权限（无修饰符）= 同包内可见

### 4.2 构造函数差异
- **this()**：调用其他构造函数（C#用`: this()`）
  ```java
  public Student() {
      this("Unknown");  // 调用另一个构造函数
  }
  ```

- **super()**：调用父类构造函数（C#用`: base()`）
  ```java
  public Student(String name) {
      super();  // 调用父类无参构造
      this.name = name;
  }
  ```

- 如果有自定义构造函数，**默认无参构造不再提供**（C#也一样）

### 4.3 final关键字
- **final变量**：常量（C#用const/readonly）
- **final方法**：不能被重写（C#用sealed）
- **final类**：不能被继承（C#用sealed）

```java
final int MAX = 100;
final void method() { }
final class MyClass { }
```

### 4.4 匿名对象
```java
new Student().getName();  // 创建匿名对象
```
C#也支持，但Java更常见

### 4.5 包装类（装箱拆箱）

| 基本类型 | 包装类 |
|----------|--------|
| int | Integer |
| char | Character |
| boolean | Boolean |
| double | Double |

```java
int x = 5;
Integer obj = x;      // 自动装箱
int y = obj;          // 自动拆箱
```

C#也有，但Java的包装类是不可变的

### 4.6 枚举类型
```java
enum Color {
    RED, BLUE, GREEN;
    
    // 可以有字段、方法、构造函数
    private String hex;
    Color() { this.hex = "000"; }
}
```

**Java枚举是类**：可以有字段、方法、构造函数（C#枚举是值类型）

### 4.7 单例模式（枚举实现）
```java
enum Singleton {
    INSTANCE;
    public void doSomething() { }
}
// 使用：Singleton.INSTANCE.doSomething();
```

---

## 第5章 多态、接口及内部类

### 5.1 类型转换差异

| 转换 | Java | C# |
|------|------|-----|
| 向上转型 | 隐式 | 隐式 |
| 向下转型 | 显式，可能ClassCastException | 显式，可能InvalidCastException |
| 类型检查 | `instanceof` | `is` |

```java
Animal a = new Dog();       // 向上转型
if (a instanceof Dog) {     // 类型检查
    Dog d = (Dog) a;        // 向下转型
}
```

### 5.2 抽象类
```java
abstract class Animal {
    abstract void makeSound();  // 抽象方法
    void sleep() { }            // 具体方法
}
```
与C#相同

### 5.3 接口差异

| 特性 | Java | C# |
|------|------|-----|
| 默认方法 | 支持（JDK8+） | 支持 |
| 静态方法 | 支持（JDK8+） | 支持 |
| 私有方法 | 支持（JDK9+） | 不支持 |
| 多继承 | 接口支持 | 接口支持 |

```java
interface Flyable {
    void fly();                    // 抽象方法
    default void land() { }        // 默认方法（JDK8+）
    static void info() { }         // 静态方法（JDK8+）
}
```

### 5.4 内部类（C#也有，但Java更复杂）

| 类型 | Java | C# |
|------|------|-----|
| 成员内部类 | 支持 | 支持 |
| 静态内部类 | 支持 | 支持 |
| 局部内部类 | 支持 | 不支持 |
| 匿名内部类 | 支持 | 用Lambda替代 |

```java
// 匿名内部类（Java特有）
button.setOnClickListener(new OnClickListener() {
    @Override
    public void onClick(View v) { }
});
```

---

## 第6章 OO概述与UML概述

### 6.1 UML图分类

| 类型 | 图名 | 用途 |
|------|------|------|
| 静态结构 | 类图、对象图、包图 | 描述系统结构 |
| 动态行为 | 状态图、活动图 | 描述行为流程 |
| 交互 | 用例图、顺序图、通信图 | 描述对象交互 |
| 实现 | 构件图、部署图 | 描述物理部署 |

---

## 第7章 用例图

### 7.1 组成元素
- **参与者(Actor)**：系统外部与系统交互的人/物/系统
- **用例(Use Case)**：系统提供的功能
- **关联**：参与者与用例之间的关系
- **系统边界**：区分系统内外

### 7.2 关系类型
- **关联**：参与者与用例之间
- **泛化**：用例之间或参与者之间的继承
- **包含(include)**：用例A必须包含用例B
- **扩展(extend)**：用例A可选扩展用例B

---

## 第8章 类图与对象图

### 8.1 类图关系

| 关系 | 符号 | 说明 | 示例 |
|------|------|------|------|
| 关联 | —— | 结构关系 | 学生-课程 |
| 依赖 | - - -> | 使用关系 | 人-工具 |
| 泛化 | ——▷ | 继承 | 狗-动物 |
| 实现 | - -▷ | 接口实现 | 类-接口 |
| 聚合 | ◇—— | 整体-部分(弱) | 部门-员工 |
| 组合 | ◆—— | 整体-部分(强) | 人-心脏 |

### 8.2 多重度
```
1      恰好一个
0..1   零或一个
*      零或多个
1..1+  一或多个
```

### 8.3 对象图
- 类图的实例快照
- 显示某一时刻对象的状态和关系
- 用于验证类图的多重度

---

## 第9章 顺序图与协作图

### 9.1 顺序图元素
- **生命线(Lifeline)**：对象存在的时间段
- **消息(Message)**：对象间的通信
- **激活(Activation)**：对象执行操作的时间段

### 9.2 消息类型
- **同步消息**：实线箭头，等待返回
- **返回消息**：虚线箭头
- **异步消息**：实线箭头(开放)，不等待

---

## 第10章 状态图

### 10.1 组成元素
- **状态(State)**：对象生命周期的某一阶段
- **转换(Transition)**：状态间的变化
- **事件(Event)**：触发转换的条件
- **动作(Action)**：转换时执行的操作

### 10.2 状态表示
```
状态名
entry/ 入口动作
do/ 内部活动
exit/ 出口动作
事件[条件]/ 动作
```

### 10.3 特殊状态
- **初始状态**：● (实心圆)
- **终止状态**：◎ (同心圆)

---

## 第11章 活动图与包图

### 11.1 活动图元素
- **活动(Activity)**：工作流中的步骤
- **控制流(Control Flow)**：活动间的顺序
- **判断节点(Decision)**：分支条件
- **合并节点(Merge)**：分支合并
- **分叉节点(Fork)**：并行开始
- **汇合节点(Join)**：并行同步
- **泳道(Swimlane)**：责任分区

### 11.2 包图
- 用于组织UML模型元素
- 表示包之间的依赖关系
- 类似于Java的包或C#的命名空间

---

## 第12章 物理视图

### 12.1 构件图
- **构件(Component)**：可重用的软件单元
- **接口(Interface)**：构件对外的服务
  - 提供接口(Provided)：构件提供的服务
  - 需求接口(Required)：构件需要的服务

### 12.2 部署图
- **节点(Node)**：计算资源（服务器、设备）
- **连接(Connection)**：节点间的通信路径
- 显示软件在硬件上的部署

---

## 重点总结

### Java特有概念
1. 包访问权限（无修饰符）
2. 静态导入
3. 枚举是类（可有字段方法）
4. 匿名内部类
5. 检查型异常
6. 数组.length是属性
7. ==比较引用，equals比较值

### UML考试重点
1. 类图的6种关系
2. 用例图的4种关系
3. 顺序图的消息类型
4. 状态图的状态转换
5. 活动图的分支与并行
