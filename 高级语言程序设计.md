# 高级语言程序设计

## 课程信息
- **试卷构成**：Java 50% + UML 50%
- **成绩构成**：期末70% + 平时30%
- **常见设计模式**：工厂模式、单例模式、适配器模式

## 课程内容
### 第一部分：Java语言基础
- 讲授Java知识体系最重要最基础的内容
- 进一步提高还需要学习很多知识

### 第二部分：UML建模
- 解决软件系统的分析、设计、概念表达问题
- 利用UML建模协助开发者设计、实现和维护Java程序

### 第三部分：建模的应用
- 通过设计模式例子讲解UML建模和Java语言程序设计的实际应用
- 由于课时关系，这部分内容需要自学

## 课程目标
- 朴素的OOP → 系统化的OOP
- 传统OO技术 → 现代的OO技术

---

## 第1章 Java语言基础

### 1.1 Java语言概述

#### 1.1.1 Java语言的发展历史
- 1990年代初：Oak语言，James Gosling设计
- 1993年：改名为Java语言
- 1995年：正式推出Java语言和平台
- 2010年：Oracle收购Sun Microsystems
- 2017年：修改版本发布策略，每6个月发布一个版本
- 2026年2月：最新Java版本是Java 25（LTS版本）
- 目前LTS版本：Java 8, 11, 17, 21, 25

#### 1.1.2 Java语言的特点
1. **编译和解释并存**
2. **平台独立性和可移植性**（Write Once, Run Anywhere）
3. **面向对象**
4. **健壮且安全**
5. **分布式**
6. **多线程**

**Java与C++的区别**：
- 没有预处理功能（用import）
- 没有structure、union、typedef
- 没有不属于类成员的函数，没有指针和多重继承
- 禁用goto（保留关键字）
- 没有操作符重载
- 没有全局变量

#### 1.1.3 Java虚拟机(JVM)
- **字节码**：JVM的指令组，可跨平台运行
- **解释器**：JVM的一部分，逐条翻译执行字节码
- **JIT即时编译器**：提高Java程序性能
- **混合模式**：最常用运行模式，结合解释执行和编译执行

#### 1.1.4 面向对象编程概述
- **自顶向下编程**：结构化程序设计
- **自底向上编程**：封装为"模块"，逐步构筑系统
- **对象**：包含数据和函数的模块
- **封装**：将数据（属性）和代码（方法）绑定到一起
- **多态性**：不同对象以不同方式响应相同消息
- **类**：对大量对象的抽象
- **继承**：子类继承父类的所有属性和方法

#### 1.1.5 基本Java应用程序
```java
public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello World!");
    }
}
```
- 运行方法：`javac HelloWorld.java` → `java HelloWorld`

### 1.2 数据类型、变量和表达式

#### 1.2.1 Java符号集
5种类型：
1. **标识符**
2. **关键字**
3. **常量**
4. **运算符**
5. **分隔符**

#### 1.2.2 注释
- 单行注释：`//`
- 多行注释：`/* */`
- 文档注释：`/** */`

#### 1.2.3 标识符
- 组成：字母、数字、下划线(_)、美元符号($)
- 开头：字母、下划线或美元符号，不能以数字开头
- 大小写敏感
- 关键字不能当标识符使用

**书写规范**：
1. 不宜过短或过长
2. 减少使用除下划线、美元符号和字母之外的字符
3. 最好只使用下划线
4. 每个单词开头字母大写，如HelloWorld

### 1.3 关键字
- 又叫保留字，Java本身使用的标识符
- const和goto保留但未使用
- true、false和null是保留字但不是关键字

**Java关键字**：
```
abstract   class     extends   import     private   switch     volatile
assert     const     final     instanceof protected synchronized while
boolean    continue  finally   int        public    this
break      default   float     interface  return    throw
byte       do        for       long       short     throws
case       double    goto      native     static    transient
catch      else      if        new        strictfp  try
char       enum      implements package    super     void
```

### 1.4 常量（字面量）

#### 基本数据类型
| 数据类型 | 类型名 | 占用位数 | 取值范围 | 默认值 |
|---------|--------|----------|----------|--------|
| 布尔型 | boolean | 8(1 byte) | true, false | false |
| 字符型 | char | 16(2 bytes) | '\u0000'～'\uffff' | '\u0000' |
| 字节型 | byte | 8(1 byte) | -128～127 | 0 |
| 短整型 | short | 16(2 bytes) | -32768～32767 | 0 |
| 整型 | int | 32(4 bytes) | -2^31～2^31-1 | 0 |
| 长整型 | long | 64(8 bytes) | -2^63～2^63-1 | 0 |
| 浮点型 | float | 32(4 bytes) | 约1.4E-45～3.4028235E38 | 0.0f |
| 双精度型 | double | 64(8 bytes) | 约4.9E-324～1.7976931348623157E308 | 0.0d |

#### 引用数据类型
- 类、数组、接口等
- 引用数据类型的默认值是null

#### 变量初始化
- **类的成员变量**：有默认值（new分配时清零）
- **方法局部变量**：没有默认值（栈中分配）

#### 常量表示方法
1. **整数**：
   - 十进制：63
   - 八进制：077
   - 十六进制：0x3f
   - long型：100000L

2. **浮点数**：
   - float：314.5f
   - double：3.145 或 3.145D
   - 科学计数法

3. **布尔型**：
   - boolean flag = true
   - boolean flag = false

4. **字符常量**：
   - 单引号内：char ch='5'; char ch2='国';
   - 字符转义

5. **字符串常量**：
   - 双引号内：String s="Hello"
   - 字符串转义：System.out.println("Sir,\nwhere are you?")

### 1.5 运算符以及分隔符

#### 分隔符
- 大括号：{}
- 分号：;
- 逗号：,
- 冒号：:
- 点号：.
- 括号：()、[]、<>

#### 运算符分类
1. **算术运算操作符**：+、-、*、/、%、++、--
2. **关系操作符**：>、>=、<、<=、==、!=
3. **位操作符**：>>、<<、>>>、&、|、^(异或)、~(取反)
4. **逻辑操作符**：&、|、!、^(异或)、&&、||
5. **赋值操作符**：=、+=、-=、*=、/=、%=、&=、|=、^=、<<=、>>=、>>>=
6. **其它操作符**：?:、[]、.、()、(type)、new、instanceof

#### 特殊运算符说明
1. **逻辑与位操作符**：根据操作数判定是何种运算符
2. **&&、||**：短路与、或
3. **&、|**：不短路与、或
4. **instanceof**：检查对象实例是否是特定类或其子类的实例
5. **String连接**：唯一的"运算符重载"，String s="Hello"+5;
6. **>>**：带符号右移，符号位扩展
7. **>>>**：无符号右移，以0填充
8. **强制类型转换**：(type) expression，如(float)x/2

### 1.6 变量与声明

#### 变量声明格式
```java
<type> <identifier [,identifier]>;
```
- 可以在声明时初始化：int i, j=0;
- 可以随时定义变量，不必集中在执行语句之前

#### 数组类型声明
```java
int count[];      // 一维数组
char ch[][];      // 二维数组
float f[];

// 等价形式
int[] count;      // 一维数组
char[][] ch;      // 二维数组
float[] f;
```
- 声明时不指定数组大小
- 需要使用new运算符分配存储空间
- 数组是引用类型

#### 引用变量声明
```java
class DataOnly {
    int i;
    double d = 5.;
    boolean b;
}

DataOnly data = new DataOnly();
data.i = 47;
data.d = 1.1;
data.b = false;
```
- 基本数据变量：直接存储值
- 引用变量：存储对象的引用

### 1.7 变量的作用域

#### 作用域定义
- 标识符在程序的某一个部分有效
- 从变量定义处开始，到所在块结束为止
- 块由一对大括号"{}"定义

#### 作用域范围
- **类成员变量**：作用域为整个类
- **方法局部变量**：作用域为所在的代码块

#### 变量隐藏
- 类中方法的局部变量会隐藏类的成员变量
- 与C语言不同：Java不允许在两个嵌套的复合语句内声明两个同名的变量

**示例**：
```java
class App {
    static int j = 5;
    public static void main(String args[]) {
        func();
    }
    public static void func() {
        int j = 6;  // 隐藏了类的成员变量j
        System.out.println("j=" + j);  // 输出6
    }
}
```

### 1.8 表达式及运算符的优先级别

#### 表达式定义
- 由变量、操作符以及方法调用所构成的结构
- 以";"结尾的一段代码即为一个表达式
- 每个表达式都有类型

#### 赋值表达式副作用
```java
int i = 5;
int j = (i++) + i;  // 结果确定，但易混淆
```
- Java编译器按固定顺序处理表达式
- 单线程情况下结果是确定的
- 并发情况下不可预测
- 避免这种代码风格

#### 类型转换
1. **扩展转换**：
   - byte → char → short → int → long → float → double
   - 不损失信息（整数到整数，float到double）

2. **窄化转换**：
   - double → float → long → int → short → byte → char
   - 可能丢失信息

3. **赋值转换**：表达式类型转换为变量类型
4. **方法调用转换**：方法参数转换
5. **强制转换**：(type) expression，如(float)5.0
6. **字符串转换**：任何类型都可转换为字符串
7. **数字提升**：
   - 一元数字提升：byte/short/char → int
   - 二元数字提升：算术运算符操作数转换为共同类型

#### 强制类型转换
```java
(欲转换的数据类型)变量名
```
- 将较长的数据转换成较短的数据时需要进行强制类型转换
- 否则发生编译错误

#### 字符串与数值转换
```java
// 字符串转数值
String MyNumber = "1234.56";
float MyFloat = Float.parseFloat(MyNumber);

// 数值转字符串
int MyInt = 1234;
String MyString = "" + MyInt;  // 自动转换
```

### 1.9 对象与类

#### 类与对象关系
- **类**：抽象数据类型，构造对象的模板
- **对象**：类的具体实例(instance)

#### 1.9.1 类类型的声明
```java
[public] [abstract | final] class 类名称
[extends 父类名称]
[implements 接口名称列表]
{
    变量成员声明及初始化；
    方法声明及方法体；
}
```

**关键字**：
- `class`：声明类
- `extends`：继承父类
- `implements`：实现接口

**修饰符**：
- `public`：公有类
- `abstract`：抽象类
- `final`：终结类

#### 1.9.2 定义类中的属性
```java
[public | protected | private]
[static] [final] [transient] [volatile]
变量数据类型 变量名1[=变量初值],
            变量名2[=变量初值], ...;
```

**格式说明**：
- `public/protected/private`：访问控制符
- `static`：静态成员变量
- `final`：值不能被修改
- `transient`：临时状态（不必持久化）
- `volatile`：共享变量
- 显式初始化：声明时赋值

#### 1.9.3 生成对象
```java
// 声明对象变量
DataOnly data;

// 创建对象
data = new DataOnly();

// 合并声明和创建
DataOnly data = new DataOnly();
```

**访问对象成员**：
```java
objectReference.member
data.i = 47;
data.d = 1.1;
data.b = false;
```

**多个对象**：
```java
DataOnly data1, data2;
data1 = new DataOnly();
data2 = new DataOnly();
data1.i = 47;
data2.i = 48;  // 存储空间隔离
```

#### 1.9.4 成员函数的定义
```java
[public | protected | private]
[static] [final] [abstract] [native] [synchronized]
返回类型 方法名([参数列表]) [throws exceptionList]
{
    方法体
}
```

**方法调用**：
```java
<对象名>.<方法名>([参数列表])
int x = a.f();  // 调用对象a的方法f()
```

**参数传递**：
- **值传递**：参数类型为基本数据类型时
- **引用传递**：参数类型为引用变量时（对象类型或数组）

**示例**：
```java
int storage(String s) {
    return s.length() * 2;  // 计算字符串占用的字节数
}
```

### 1.10 Java的包

#### 包的作用
- 每个类都包含在包中
- 未明确放入包中的类位于"默认"包
- 标准类在包中：如String和System在java.lang包中
- java.lang中的类自动导入

#### 导入包
```java
import java.util.ArrayList;  // 导入ArrayList类
import java.util.*;          // 导入java.util包中所有类
```

#### 包名与路径
- 包名必须唯一，推荐使用反向互联网域名：cn.edu.nwpu.cs
- 包名解析到机器上的目录
- 环境变量CLASSPATH包含搜索.class文件的根目录

#### 冲突解决
```java
import cn.edu.nwpu.simple.*;
import java.util.*;

// 冲突：两个包都有Vector类
// 解决方案1：使用限定名
java.util.Vector v = new java.util.Vector();

// 解决方案2：import指定类名，不使用*
```

#### 自定义工具库
```java
package cn.edu.nwpu.util;
import java.io.*;

public class Print {
    // Print with a newline:
    public static void print(Object obj) {
        System.out.println(obj);
    }
    // Print a newline by itself:
    public static void print() {
        System.out.println();
    }
    // Print with no line break:
    public static void printnb(Object obj) {
        System.out.print(obj);
    }
}

// 使用静态导入
import static cn.edu.nwpu.util.Print.*;
public class PrintTest {
    public static void main(String[] args) {
        print("Available from now on!");
        print(100);
        print(100L);
        print(3.14159);
    }
}
```

### 1.11 Java的代码组织

#### 1.11.1 Java源代码文件的构成
- 一个java文件称作一个编译单元，扩展名必须是.java
- 源文件中可以有多个类，但最多只能有一个public类
- 如果有public类，源文件的名字必须与这个public类的名字相同
- 如果没有public类，源文件的名字任意命名
- 编译.java文件时，会为每个类产生一个.class输出文件

#### 1.11.2 包名与路径
- 使用分层文件结构管理.class文件
- 包名必须唯一，推荐使用反向互联网域名
- 环境变量CLASSPATH包含搜索.class文件的根目录

**示例**：
```
// 包声明
package cn.edu.nwpu.simple;

// 文件位置
C:\DOC\Java\cn\edu\nwpu\simple\List.java
C:\DOC\Java\cn\edu\nwpu\simple\Vector.java

// CLASSPATH设置
CLASSPATH=.;D:\JAVA\LIB;C:\DOC\Java
```

#### 1.11.3 冲突
- 如果通过"*"导入两个库并且它们包含相同的名称，会发生冲突
- 使用限定名解决冲突
- 或者import指定类名，不使用*

#### 1.11.4 自定义工具库
- 创建自己的工具库减少重复代码
- 静态导入(Static Import)：从Java 5开始引入，允许直接导入一个类的静态成员

### 1.12 文本输入和输出

#### 1.12.1 基本输出和格式化输出
```java
System.out.print(x);      // 不带换行的输出
System.out.println(x);    // 带换行的输出
System.out.printf();      // 格式化输出，类似C语言的printf()
```

**特殊格式**：
- `%n`：输出换行（平台无关）
- `%%`：输出百分号
- `\\`：输出反斜杠

#### 1.12.2 文本输入例子
```java
import java.util.Scanner;

public class Interest2WithScanner {
    public static void main(String[] args) {
        Scanner stdin = new Scanner(System.in);  // 创建输入类Scanner
        double principal;  // 投资金额
        double rate;       // 年利率
        double interest;   // 一年的收益
        
        System.out.print("Enter the initial investment: ");
        principal = stdin.nextDouble();
        
        System.out.print("Enter the annual interest rate (as a decimal): ");
        rate = stdin.nextDouble();
        
        interest = principal * rate;  // 计算当年的收益
        principal = principal + interest;  // 累计投资金额
        
        System.out.printf("The amount of interest is $%1.2f%n", interest);
        System.out.printf("The value after one year is $%1.2f%n", principal);
    }
}
```

**输入方法**：
- `stdin.nextDouble()`：读取双精度浮点数
- `stdin.nextLong()`：读取长整型
- `stdin.nextBoolean()`：读取布尔类型（只接受"true"或"false"）

### 1.13 Java开发环境

#### Java版本
- 2025年9月：Java 25发布，最新的LTS版本
- 每六个月进行一次新特性发布，每三年进行一次LTS发布
- 目前主要有Oracle的JDK和OpenJDK两个版本
- Linux系统上一般使用OpenJDK

#### JDK、JRE、JVM
- **JDK**：Java开发工具包，包括编译器(javac)、JRE、调试器等
- **JRE**：Java运行时环境，包含JVM和Java类库
- **JVM**：Java虚拟机，执行字节码

#### OpenJDK
- 从Java 11开始，Oracle对商业用途的Java产品实施了新的许可政策（付费）
- 很多项目采用OpenJDK
- 最流行的是AdoptOpenJDK（现在称为Adoptium）
- 建议使用OpenJDK 8或者17

#### 下载方法
- https://adoptium.net/temurin/releases/?os=windows
- MSI：Windows安装包，使用简单
- ZIP：绿色版，需要解压后自己设置环境变量

#### 开发工具
1. **Eclipse**：免费，推荐
2. **IntelliJ IDEA**：收费，当前最流行的工具（有学生免费版）
3. **NetBeans**：免费
4. **VS Code**：免费
5. **NotePad++**：免费

#### JShell
- Java 9新特性，REPL工具
- 类似于python解释器，允许交互式执行Java代码
- 帮助快速测试代码
- 不是IDE的替代品

---

## 第2章 控制语句

### 2.1 程序块、循环和分支

#### 2.1.1 程序块
- 又叫复合语句，用{}把一些语句括起来构成复合语句
- 在可以出现语句的任何地方都可以合法地使用程序块
- 与C++不同：Java不允许在两个嵌套的复合语句内声明两个同名的变量

**示例**：
```java
public class E0201DeclarBlockVariables {
    public static void main(String[] args) {
        // int k=0; 这个k和下面嵌套的语句块内k同名，会引起编译错误。
        {
            int k = 5;
            System.out.print(k);
        }
        { // 这个语句块和上面语句块并列，同名变量k没有问题。
            int k = 6;
            System.out.println("," + k);
        }
    }
}
```

#### 2.1.2 while循环
- 实现"当型"循环
- 先判断条件表达式的值，为真则执行循环体
- 循环体执行完后再无条件转向条件表达式做计算与判断

**语法格式**：
```java
while(条件表达式) {
    // 循环体
    <statements>
}
```

**示例：求1-100的和**：
```java
public class E0202WhileDemoSum {
    public static void main(String[] args) {
        // 使用while循环求1~100的和
        int sum = 0;
        int i = 1;
        while(i <= 100) {
            sum += i;
            i++;
        }
        System.out.println("1~100的和是：" + sum);
    }
}
// 输出结果：1~100的和是：5050
```

**中文编码问题**：
- 源代码保存的编码格式是UTF-8，不是GBK
- 解决方法：
  ```bash
  java -Dfile.encoding=UTF-8 WhileDemoSum.java
  或
  javac -encoding UTF-8 WhileDemoSum.java
  java WhileDemoSum
  ```

#### 2.1.3 do...while语句
- 实现"直到型"循环
- 首先无条件的执行一遍循环体，再来判断条件表达式的值
- 特点：循环体至少要执行一次

**语法格式**：
```java
do {
    // 循环体
} while(条件表达式);
```

**示例：用户游戏**：
```java
import java.util.Scanner;

public class E0203WhileDemoPlayGame {
    public static void main(String[] args) {
        Scanner scanin = new Scanner(System.in); // scanner for input
        boolean wantsToContinue; // 为真(true)时继续游戏
        
        do {
            Checkers.playGame(); // 函数调用
            System.out.print("Do you want to play again? "); // 提示用户输入
            String ans = scanin.nextLine(); // 等待yes或者no
            wantsToContinue = ans.equalsIgnoreCase("yes");
        } while(wantsToContinue == true);
        
        System.out.println("不玩了。");
        scanin.close();
    }
}

class Checkers {
    public static void playGame() {
        System.out.println("游戏中。。。");
        System.out.println("游戏结束。");
    }
}
```

**等价关系**：
```java
do {
    <doSomething>
} while(<boolean-expression>);

// 等价于
<doSomething>
while(<boolean-expression>) {
    <doSomething>
}

while(<boolean-expression>) {
    <doSomething>
}

// 等价于
if(<boolean-expression>) {
    do {
        <doSomething>
    } while(<boolean-expression>);
}
```

**示例：求1-100的和**：
```java
public class E0204DoWhileDemoSum {
    public static void main(String[] args) {
        // 使用do-while循环求1~100的和
        int sum = 0;
        int i = 1;
        do {
            sum += i;
            i++;
        } while(i <= 100);
        System.out.println("1~100的和是：" + sum);
    }
}
// 输出结果：1~100的和是：5050
```

### 2.2 程序设计中的算法

#### 2.2.1 伪代码和逐步细化
- **算法**：程序执行任务所采取的步骤的想法
- **伪代码**：模仿编程语言结构的非正式指令
- **逐步细化**：自顶向下，逐步添加步骤和细节

**逐步细化过程**：
1. 编写任务描述作为算法大纲
2. 细化和详细说明描述，添加步骤和细节
3. 用伪代码写出算法描述
4. 把伪代码转换为具体的程序设计语言

**示例：计算未来五年每年的投资价值**：
```
初始算法：
获取用户的初始资金和利率
计算1年后的投资价值
显示值
计算2年后的值
显示值
计算3年后的值
显示值
计算4年后的值
显示值
计算5年后的值
显示值

改进算法（使用循环）：
获取用户的初始资金和利率
while 投资期限处理（按照期限循环）：
    计算下一年之后的值
    显示值

细化算法：
提示用户输入初始资金
读取用户的输入
提示用户输入利率
读取用户的输入
years = 0
while years < 5:
    years = years + 1
    计算利息
    将利息添加到本金中
    显示值
```

#### 2.2.2 编码、编译、测试、调试
- **编码**：将算法转化为Java源代码
- **编译**：编译器检查语法错误
- **测试**：确保对所有合理的输入都得到正确的结果
- **调试**：找出bug的具体位置

**测试要点**：
- 测试各种输入组合
- 设计覆盖程序所有功能的输入组合
- 对于不合理的输入，优雅地处理
- 重点找出bug（程序处理结果错误）

**调试方法**：
- 阅读代码，设想代码执行的结果
- 设置断点，查看变量、内存、调用栈
- 插入System.out.println()，观察输出的变量值

#### 2.2.3 算法设计例题

**(1) 3N+1问题（Collatz猜想）**
- 对于任意正整数N：
  - 如果N是偶数：N = N / 2
  - 如果N是奇数：N = 3 * N + 1
- 猜想：无论选择哪个大于零的整数，序列最终都会达到1

**算法**：
```
从用户那里获取正整数N
while N is not 1:
    if N is even:
        N = N / 2
    else:
        N = 3 * N + 1
    输出N
    计数这一项
输出计数结果
```

**(2) while语句综合使用：求平均值**
- 求用户输入的一组正整数的平均值
- 用户一次输入一个整数，输入0结束

**算法**：
```
sum = 0
count = 0
读取一个整数
while 整数不为零:
    将整数加到sum
    count加1
    读取下一个整数
平均值 = sum / count
输出平均值
```

**(3) for循环综合使用：查找除数个数**
- 计算N可以被多少个不同的数整除
- 对于N的可能除数集合D={1, 2, ..., N}逐一检查

**算法**：
```
获取用户输入的整数N
divisorCount = 0
for testDivisor = 1 to N:
    if N % testDivisor == 0:
        divisorCount加1
输出divisorCount
```

**(4) if语句综合应用：三个数排序**
- 按递增顺序输出三个变量的值

**算法1**：
```
if (x < y && x < z) { // x是第一个数
    if (y < z)
        输出 x, y, z
    else
        输出 x, z, y
}
else if (x > y && x > z) { // x是最后一个数
    if (y < z)
        输出 y, z, x
    else
        输出 z, y, x
}
else { // x位于中间
    if (y < z)
        输出 y, x, z
    else
        输出 z, x, y
}
```

**算法2**：
```
if (x < y) { // x在y之前
    if (z < x) // z在最前面
        输出 z, x, y
    else if (z > y) // z在最后
        输出 x, y, z
    else // z在中间
        输出 x, z, y
}
else { // y在x前面
    if (z < y) // z在最前面
        输出 z, y, x
    else if (z > x) // z在最后
        输出 y, x, z
    else // z在中间
        输出 y, z, x
}
```

### 2.3 Java语言中的异常处理简介

#### 异常处理概述
- 处理"异常"控制流的控制结构
- 当程序执行期间发生错误时，可以"捕获"此类错误并执行指定响应
- 错误处理代码与业务代码分离

**对比C语言和Java**：
```c
// C语言
FILE* fp = fopen(fn, "rt");
if(NULL == fp) {
    perror("文件打开失败。")
    goto err_exit;
}
```

```java
// Java
try(BufferedReader reader = new BufferedReader(new FileReader(fn))) {
    // 读入文件中数据进行处理
} catch(IOException e) {
    System.err.println("文件打开失败: " + fn);
    System.err.println(e.toString());
    e.printStackTrace();
}
```

#### 2.3.1 Exceptions

**Java异常的层次结构**：
```
Throwable
├── Error
│   ├── VirtualMachineError
│   ├── OutOfMemoryError
│   └── StackOverflowError
└── Exception
    ├── RuntimeException
    │   ├── NullPointerException
    │   ├── IndexOutOfBoundsException
    │   ├── ArithmeticException
    │   └── IllegalArgumentException
    └── 检查型异常
        ├── IOException
        ├── SQLException
        └── ClassNotFoundException
```

**异常分类**：
1. **检查型异常（Checked Exceptions）**
   - 编译器会检查代码中是否对这类异常进行了处理
   - 必须通过try-catch块处理，或在方法签名中使用throws子句声明
   - 继承自Exception类，但不包括RuntimeException及其子类
   - 例子：IOException、SQLException、ClassNotFoundException

2. **非检查型异常（Unchecked Exceptions）**
   - 编译器不强制要求处理
   - 继承自RuntimeException
   - 例子：NullPointerException、IndexOutOfBoundsException、ArithmeticException

3. **错误（Errors）**
   - 表示严重的系统问题，通常不应被捕获
   - 继承自Error类
   - 例子：OutOfMemoryError、StackOverflowError、VirtualMachineError

### 2.4 数组

#### 数组概述
- 数组是一种专门的类型，通过索引方式组织一个对象列表
- 数组可以嵌套（多维数组）
- 数组是引用类型

#### 数组声明和初始化
```java
// 一维数组
int[] arr1 = new int[10];
int[] arr2 = {1, 2, 3, 4, 5};

// 二维数组
int[][] matrix1 = new int[3][4];
int[][] matrix2 = {{1, 2, 3}, {4, 5, 6}, {7, 8, 9}};
```

#### 数组操作
```java
// 访问数组元素
int value = arr[0];
arr[0] = 10;

// 获取数组长度
int length = arr.length;

// 遍历数组
for(int i = 0; i < arr.length; i++) {
    System.out.println(arr[i]);
}

// 增强for循环（foreach）
for(int element : arr) {
    System.out.println(element);
}
```

#### 数组特点
1. **固定长度**：数组一旦创建，长度不可改变
2. **相同类型**：数组中的所有元素必须是相同类型
3. **连续内存**：数组元素在内存中是连续存储的
4. **索引访问**：通过索引（从0开始）访问元素
5. **默认初始化**：数值类型初始化为0，布尔类型初始化为false，引用类型初始化为null

#### 多维数组
```java
// 声明二维数组
int[][] matrix = new int[3][4];

// 访问二维数组元素
matrix[0][0] = 1;
matrix[0][1] = 2;

// 遍历二维数组
for(int i = 0; i < matrix.length; i++) {
    for(int j = 0; j < matrix[i].length; j++) {
        System.out.print(matrix[i][j] + " ");
    }
    System.out.println();
}
```

#### 数组与集合的区别
1. **长度**：数组长度固定，集合长度可变
2. **类型**：数组可以存储基本类型和对象，集合只能存储对象
3. **性能**：数组访问速度快，集合功能更丰富
4. **灵活性**：集合提供了更多操作方法（添加、删除、查找等）

---

## 总结

### 第1章重点
1. Java语言特点：跨平台、面向对象、健壮安全
2. 基本数据类型和引用数据类型
3. 运算符和表达式
4. 变量声明和作用域
5. 类和对象的概念
6. 包和代码组织
7. 输入输出方法
8. 开发环境配置

### 第2章重点
1. 控制语句：if、switch、while、do-while、for
2. 程序块和复合语句
3. 算法设计和伪代码
4. 异常处理基本概念
5. 数组的定义和使用

### 学习建议
1. 理解面向对象编程思想
2. 掌握基本语法和控制结构
3. 练习算法设计和代码实现
4. 熟悉开发工具和环境配置
5. 注意代码规范和错误处理